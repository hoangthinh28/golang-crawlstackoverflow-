docker run -it --name "CONTAINER NAME" -h HOSTNAME image: Tạo name container cho image
docker ps
docker exit CONTAINER_ID
docker start CONTAINER_ID
docker ps -a
docker rm CONTAINER_ID
docker rm -f CONTAINER_ID :remove folk
docker image rm IMAGE_ID
docker attach CONTAINER_ID: return terminal in container 
docker exec CONTAINER_NAME ls: listed in CONTAINER_NAME list
docker exec -it CONTAINER_NAME bash : same same atach
docker save --output FILENAME.tar ff: save image file 
docker rm image ff: delete image 
docker load -i FILENAME.tar restore file into image
docker tag IMAGE_ID NEWNAMEIMAGE:TAG : new name to image
docker run -it -v PATHHOST:PATHCONTAINER IMAGE_ID
docker volume create NAMEDISK
docker volume ls: list volumn
docker volume inspect NAMEDISK: Check info NAMEDISK
docker volume rm NAMEDISK
docker run -it --name "CONTAINER_NAME" --mount source=DISK, target=PATHCONTAINER IMAGE_ID
docker network ls
docker network inspect NAME_NETWORK